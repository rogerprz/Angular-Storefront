export interface Product {
  id: 1;
  name: String;
  selected: Boolean;
  quantity: Number;
  minimum_quantity: Number;
  quantity_editable: Number;
  removable_from_session: Boolean;
  format: String;
}
